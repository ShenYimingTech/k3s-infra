apiVersion: cert-manager.io/v1
kind: Certificate
metadata: {name: edge-wildcard, namespace: proxy}
spec:
  secretName: edge-wildcard-tls
  issuerRef: {name: letsencrypt-prod, kind: ClusterIssuer}
  dnsNames: ["*.edge.k4s.live", "edge.k4s.live"]
