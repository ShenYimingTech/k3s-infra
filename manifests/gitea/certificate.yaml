apiVersion: cert-manager.io/v1
kind: Certificate
metadata: {name: git, namespace: gitea}
spec:
  secretName: git-tls
  issuerRef: {name: letsencrypt-prod, kind: ClusterIssuer}
  dnsNames: ["git.k4s.live"]
