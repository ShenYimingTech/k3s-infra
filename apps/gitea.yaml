apiVersion: argoproj.io/v1alpha1
kind: Application
metadata: {name: gitea, namespace: argocd, finalizers: [resources-finalizer.argocd.argoproj.io]}
spec:
  project: default
  source: {repoURL: git@github.com:ShenYimingTech/k3s-infra.git, targetRevision: main, path: manifests/gitea}
  destination: {server: https://kubernetes.default.svc, namespace: gitea}
  syncPolicy:
    automated: {prune: true, selfHeal: true}
    syncOptions: [CreateNamespace=true]
